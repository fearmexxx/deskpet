{
  "name": "deskpet",
  "version": "1.1.0",
  "description": "DeskPet extension source with bundle utilities",
  "main": "background.js",
  "type": "module",
  "scripts": {
    "build": "esbuild src/privy-entry.js --bundle --minify --outfile=privy-sdk.min.js --format=iife --global-name=PrivySDK"
  },
  "dependencies": {
    "@privy-io/js-sdk-core": "^0.65.3"
  },
  "devDependencies": {
    "esbuild": "^0.20.1"
  }
}
